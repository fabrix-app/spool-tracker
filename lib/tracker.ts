import { FabrixApp } from '@fabrix/fabrix'

export const Tracker = {
  /**
   * Create the Stores
   */

  init: (app: FabrixApp) => {
    return Promise.resolve()
  },

  /**
   * Unload the Stores
   */
  unload: (app: FabrixApp) => {
    return Promise.resolve()
  }
}
